#starCanvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1; /* Оставляем -1, чтобы быть позади всего */
    background: radial-gradient(ellipse at bottom, #1B2735 0%, #090A0F 100%);
    display: block;
    pointer-events: none; /* --- ЭТО ГЛАВНОЕ --- */
    /* Запрещаем канвасу перехватывать клики, они "проходят" сквозь него к кнопкам */
}

.main-content { /* Или класс вашего основного контейнера */
    position: relative;
    z-index: 1;
    /* Остальные стили */
}
