body.loaded .hud,
body.loaded .balance,
body.loaded .stars,
body.loaded .case-container,
body.loaded .airplane-container,
body.loaded .timer,
body.loaded button {
  opacity: 1;
  transform: translateY(0);
  transition: all 1s ease;
}

.hud,
.balance,
.stars,
.case-container,
.airplane-container,
.timer,
button {
  opacity: 0;
  transform: translateY(20px);
}
