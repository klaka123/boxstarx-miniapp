.case-wrapper {
  overflow: hidden; /* Скрываем всё, что выходит за границы */
  width: 300px; /* Ширина видимой области */
  border: 2px solid gold;
  border-radius: 10px;
}

.case-items {
  display: flex;
  gap: 10px;
  animation: scroll 5s linear infinite; /* Бесконечная прокрутка */
}

.case-item {
  min-width: 80px;
  height: 80px;
  background: rgba(255,215,0,0.2);
  border: 1px solid gold;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
}

@keyframes scroll {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); } /* Сдвигаем на половину ширины дублированного списка */
}

/* Важно: В HTML нужно продублировать список предметов, чтобы не было разрыва */
/* Например: <div class="case-items"> (предмет1, предмет2, предмет3, предмет1, предмет2, предмет3) </div> */
