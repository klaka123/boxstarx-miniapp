window.addEventListener('load', () => {
  setTimeout(() => {
    document.body.classList.add('loaded');
    document.getElementById('rocketLauncher').remove();
  }, 3000); // совпадает с анимацией ракеты
});
